#!/usr/bin/env node
/**
 * A tool for executing all of the `acceptance-tests` tests and formatting
 * their results into grokkable output.
 */

var processArguments = require('../lib/processArguments');

(function runTests(){
	processArguments.runIt();
})();
